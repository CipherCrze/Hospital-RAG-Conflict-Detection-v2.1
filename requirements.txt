# Hospital RAG — Conflict Detection Q&A System
# Python dependencies pinned to versions used in development (Table 4.2)
# Install: pip install -r requirements.txt
#
# Note: torch is the CPU-only build (~700 MB vs ~2.5 GB for CUDA build)
# Install separately: pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML / NLP
sentence-transformers==2.7.0
transformers==4.41.2
torch>=2.9.0

# Vector Database
chromadb==0.5.3

# LLM API
google-generativeai==0.7.2

# Frontend / UI
streamlit==1.35.0

# Document loading
PyMuPDF==1.24.5
python-docx==1.1.2

# Text splitting
langchain-text-splitters==0.2.1

# Utilities
numpy==1.26.4
python-dotenv==1.0.1

# LangChain ecosystem (used in legacy ingestion.py / rag_pipeline.py)
langchain>=0.3.0
langchain-google-genai
langchain-community
langchain-core
